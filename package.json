{
  "private": true,
  "description": "haltr monorepo — Rust source + npm package",
  "scripts": {
    "build": "cd exp/20260325-001_haltr-rust-rewrite/sandbox/haltr-rs 2>/dev/null && cargo build --release || cargo build --release",
    "test": "cargo test"
  }
}
